📖 คู่มือการใช้ Git สำหรับทีม (Git Workflow)

เอกสารนี้รวบรวมคำสั่งและขั้นตอนการทำงานร่วมกัน เพื่อป้องกันไม่ให้งานทับกันและลดข้อผิดพลาดในการรวมโค้ด

🚀 กฎเหล็กของทีม (Golden Rules)

ห้าม Push งานเข้า main (หรือ master) โดยตรงเด็ดขาด

ต้อง git pull อัปเดตโค้ดล่าสุดจากเพื่อนก่อนเริ่มงานทุกครั้ง

ตั้งชื่อ Branch ให้สื่อความหมาย เช่น feature/login-page, fix/navbar-bug

🛠 1. การเริ่มงานใหม่ (Create & Switch Branch)

ทุกครั้งที่จะทำฟีเจอร์ใหม่ หรือแก้บั๊ก ให้สร้าง "ห้องทำงานส่วนตัว" (Branch) เสมอ

1.1 ดึงโค้ดล่าสุดจาก Main ก่อนเสมอ

git checkout main        # กลับไปที่ main
git pull origin main     # ดึงโค้ดล่าสุดจาก Server ลงมา


1.2 สร้าง Branch ใหม่และสลับเข้าไปทันที
ใช้คำสั่ง checkout -b ตามด้วยชื่อ Branch

# ตัวอย่าง: สร้าง branch ชื่อ feature/register
git checkout -b feature/register


ตอนนี้คุณอยู่ใน Branch feature/register แล้ว เขียนโค้ดได้เต็มที่ ไม่กระทบคนอื่น

💾 2. การบันทึกงาน (Add & Commit)

เมื่อเขียนโค้ดเสร็จแล้ว หรือต้องการเซฟงานระหว่างทาง

git add .                      # เลือกไฟล์ทั้งหมดที่จะเซฟ
git commit -m "ทำหน้า Register เสร็จแล้ว เพิ่มปุ่ม Submit"  # บันทึกพร้อมข้อความอธิบาย


Tip: ข้อความ Commit ควรเขียนให้รู้เรื่องว่าทำอะไรไป

☁️ 3. การส่งงานขึ้น Server (Push)

เมื่อมั่นใจว่างานใน Branch เราเสร็จแล้ว ให้ส่งขึ้น GitHub/GitLab

# รูปแบบ: git push origin <ชื่อ-branch-เรา>
git push origin feature/register


หลังจาก Push แล้ว ให้ไปกด Create Pull Request (PR) ในเว็บ GitHub เพื่อรอคนอื่นรีวิวและรวมโค้ด

🔄 4. การดึงงานเพื่อนมาดูหรืออัปเดต (Pull)

กรณี 4.1: เพื่อน Push งานขึ้นไปแล้ว เราอยากดึงมาที่เครื่องเรา

git pull origin main     # ถ้าเพื่อนรวมโค้ดเข้า main แล้ว
# หรือ
git pull origin feature/friend-branch  # ถ้าอยากดึง branch เพื่อนมาดู


กรณี 4.2: ต้องการเปลี่ยนไปดูงาน Branch อื่น

git checkout feature/another-page


(ถ้า Git แจ้งเตือน error ว่าเปลี่ยนไม่ได้ ให้ Commit งานตัวเองให้เรียบร้อยก่อน หรือใช้ git stash เพื่อซ่อนงานชั่วคราว)

🆘 สรุปคำสั่งที่ใช้บ่อย (Cheat Sheet)

เช็คสถานะไฟล์
git status

ดูว่ามี Branch อะไรบ้าง
git branch

สร้าง Branch ใหม่
git checkout -b <ชื่อ>

เปลี่ยน Branch
git checkout <ชื่อ>

ดึงโค้ดล่าสุด
git pull

ส่งโค้ดขึ้น Server
git push origin <ชื่อ branch>

ยกเลิกการแก้ไขไฟล์ (อันตราย)
git checkout -- <ชื่อไฟล์>
Happy Coding! 💻✨
